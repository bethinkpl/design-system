name: Create envfile

on: [ push ]

jobs:
  create-envfile:
    runs-on: ubuntu-latest
    steps:
      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1.3
        with:
          JSON_BIN_X_MASTER_KEY: ${{ secrets.JSON_BIN_X_MASTER_KEY }}