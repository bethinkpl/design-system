import { Meta, Story, Props } from '@storybook/addon-docs';
import Colors from './Colors';
import rawColors from '../../../styles/settings/colors/_raw.json';

<Meta title="foundations/Colors/Raw" component={Colors} />

# Card

<Story
	name="Raw"
	args={{
		colorsList: rawColors,
	}}
	argTypes={{
		colorsList: Object,
	}}
>
	{(args) => {
		return {
			components: { Colors },
			template: '<colors :colorsList="colorsList"></colors>',
			props: Object.keys(args),
		};
	}}
</Story>

## Props

<Props of={Colors} />
