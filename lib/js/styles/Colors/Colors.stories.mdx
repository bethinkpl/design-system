import { Meta, Story, Props } from '@storybook/addon-docs';
import ColorsList from './../ColorsList';
import rawColors from '../../../styles/settings/colors/_raw.json';

<Meta title="foundations/Colors/Raw" component={ColorsList} />

# Card

<Story
	name="Raw"
	args={{
		colorsLists: [{ title: 'Raw', list: rawColors, disabled: 'theme' }],
	}}
	argTypes={{
		colorsLists: Array,
	}}
>
	{(args) => {
		return {
			components: { ColorsList },
			template: '<ColorsList :colorsLists="colorsLists"></ColorsList>',
			props: Object.keys(args),
		};
	}}
</Story>

## Props

<Props of={ColorsList} />
