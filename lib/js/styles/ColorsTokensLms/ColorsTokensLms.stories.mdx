import { Meta, Story, Props } from '@storybook/addon-docs';
import ColorsList from './../ColorsList';
import tokensWnl from '../../../styles/settings/colors/_tokens.json';

<Meta title="foundations/Tokens/ColorsLms" component={ColorsList} />

# Card

<Story
	name="ColorsLms"
	args={{
		colorsLists: [{ title: 'Tokens LMS', list: tokensWnl }],
	}}
	argTypes={{
		colorsLists: Array,
	}}
>
	{(args) => {
		return {
			components: { ColorsList },
			template: '<ColorsList :colorsLists="colorsLists"></ColorsList>',
			props: Object.keys(args),
		};
	}}
</Story>

## Props

<Props of={ColorsList} />
