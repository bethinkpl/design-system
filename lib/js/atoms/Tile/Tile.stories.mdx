import { Meta, Story, Props } from '@storybook/addon-docs';
import Tile from 'design-system/lib/js/atoms/Tile';
import { ICONS } from 'design-system/lib/js/atoms/Icon/Icon.consts';

<Meta title='atoms/Tile' component={Tile} />

#Tile

**Tile component**

<Story name='interactive'
  args={{
      interactive: false,
      additionalText: '',
      text: 'this is a text text',
      eyebrowText: 'this is an eyebrowText text',
   }}
  argTypes={{
      iconLeft: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: 'FA_CALENDAR_ALT' },
      iconRight: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: null },
  }}>
>
	{(args) => {
		return {
			components: { Tile },
			props: Object.keys(args),
			template: '<tile :text="text" :eyebrowText="eyebrowText" :interactive="interactive" :iconLeft="ICONS[iconLeft]" :additionalText="additionalText" :iconRight="ICONS[iconRight]" />',
            created() {
              this.ICONS = ICONS;
            }
        };
	}}
</Story>

## Props

<Props of={WnlSelect} />
