import { Meta, Story, Props } from '@storybook/addon-docs';
import Tile from 'design-system/lib/js/atoms/Tile';
import { ICONS } from 'design-system/lib/js/atoms/Icon/Icon.consts';

<Meta title='atoms/Tile' component={Tile} />

# Select

**Nazwa opisowa komponentu**

<Story name='interactive'
  args={{
      clickable: true,
      eyebrow: 'this is eyebrow text',
      text: 'this is main text',
   }}
  argTypes={{
      rightText: {control: { type: 'text' }, defaultValue: '' },
      leftIcon: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: 'FA_CALENDAR_ALT' },
  }}>
>
	{(args) => {
		return {
			components: { Tile },
			props: Object.keys(args),
			template: '<tile :text="text" :eyebrow="eyebrow" :clickable="clickable" :leftIcon="ICONS[leftIcon]" :rightText="rightText"/>',
            created() {
              this.ICONS = ICONS;
            }
        };
	}}
</Story>

## Props

<Props of={WnlSelect} />
