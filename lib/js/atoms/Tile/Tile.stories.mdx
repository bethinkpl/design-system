import { Meta, Story, Props } from '@storybook/addon-docs';
import Tile from 'design-system/lib/js/atoms/Tile';
import { ICONS } from 'design-system/lib/js/atoms/Icon/Icon.consts';

<Meta title='atoms/Tile' component={Tile} />

#Tile

**Tile component**

<Story name='interactive'
  args={{
      interactive: false,
      additionalText: '',
      text: 'this is a text text',
      eyebrowText: 'this is an eyebrowText text',
   }}
  argTypes={{
      leftIcon: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: 'FA_CALENDAR_ALT' },
      rightIcon: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: null },
  }}>
>
	{(args) => {
		return {
			components: { Tile },
			props: Object.keys(args),
			template: '<tile :text="text" :eyebrowText="eyebrowText" :interactive="interactive" :leftIcon="ICONS[leftIcon]" :additionalText="additionalText" :rightIcon="ICONS[rightIcon]" />',
            created() {
              this.ICONS = ICONS;
            }
        };
	}}
</Story>

## Props

<Props of={WnlSelect} />
