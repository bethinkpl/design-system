import { Meta, Story, Props } from '@storybook/addon-docs';
import Tile from 'design-system/lib/js/atoms/Tile';
import { ICONS } from 'design-system/lib/js/atoms/Icon/Icon.consts';

<Meta title='atoms/Tile' component={Tile} />

# Select

**Nazwa opisowa komponentu**

<Story name='interactive'
  args={{
      clickable: true,
      text: '',
      eyebrow: 'this is an eyebrow text',
      title: 'this is a title text',
   }}
  argTypes={{
      left: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: 'FA_CALENDAR_ALT' },
      right: {control: { type: 'select', options: [null, ...Object.keys(ICONS)] }, defaultValue: null },
  }}>
>
	{(args) => {
		return {
			components: { Tile },
			props: Object.keys(args),
			template: '<tile :clickable="clickable" :left="ICONS[left]" :text="text" :right="ICONS[right]"><span slot="eyebrow">{{ eyebrow }}</span><span slot="default">{{ title }}</span></tile>',
            created() {
              this.ICONS = ICONS;
            }
        };
	}}
</Story>

## Props

<Props of={WnlSelect} />
