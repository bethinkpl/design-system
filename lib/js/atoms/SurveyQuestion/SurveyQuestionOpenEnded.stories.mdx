import { Meta, Story, Props } from '@storybook/addon-docs';
import SurveyQuestionOpenEnded from './SurveyQuestionOpenEnded';

<Meta title='atoms/SurveyQuestionOpenEnded' component={SurveyQuestionOpenEnded} />

# SurveyQuestionOpenEnded

<Story name='interactive'
	   args={{
		   label: 'Main question write here if it\'s long it will collapse.',
		   value: 'value',
		   explanation: 'Wyczerpujące materiały dają poczucie pełnego zrozumieina, przy jednoczesnym usystematyzowaniu informacji. Wpływa na to nie tylko ich jakość, ale też ilość.',
		   placeholder: 'Wpisz swoją odpowiedź',
		   disabled: false,
	   }}
	   argTypes={{
		   explanation: { control: { type: 'text' } },
	   }}>
	{(args) => {
		return {
			components: { SurveyQuestionOpenEnded },
			props: Object.keys(args),
			template: '<survey-question-open-ended  :label="label" :value="value" :disabled="disabled" :placeholder="placeholder">' +
				'<div v-if="explanation" slot="explanation">{{ explanation }}</div>' +
				'</survey-question-open-ended>',
		};
	}}
</Story>

## Props

<Props of={SurveyQuestionOpenEnded} />