import { Meta, Story, Props } from '@storybook/addon-docs/blocks';

import Input from 'design-system/lib/js/atoms/Input';
import { ICONS } from 'design-system/lib/js/atoms/IconButton';

<Meta title='atoms/Input' component={Input} />

# Button

**Wspaniały** przycisk.

<Story name='with icon'
    args={{
     label: 'Label',
     value: 'Value',
     helpMessage: 'Help message',
     labelAddition: '(Label addition)',
    }}
    argTypes={{
               icon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'FA_TIMES' },
    }}>
    {(args) => {
        return {
            components: { WnlInput: Input },
            props: Object.keys(args),
            template: `<wnl-input
                :label="label"
                :value="value"
                :help-message="helpMessage"
                :labelAddition="labelAddition"
                :leftIcon="ICONS[icon]"
                :rightIcon="ICONS[icon]"
            />`,
            created() {
                this.ICONS = ICONS;
            }
        };
    }}
</Story>

<Story name='without icon'
    args={{
     label: 'Label',
     value: 'Value',
     helpMessage: 'Help message',
     labelAddition: '(Label addition)',
    }}
    argTypes={{
    }}>
    {(args) => {
        return {
            components: { WnlInput: Input },
            props: Object.keys(args),
            template: `<wnl-input
                :label="label"
                :value="value"
                :help-message="helpMessage"
                :labelAddition="labelAddition"
            />`,
            created() {
                this.ICONS = ICONS;
            }
        };
    }}
</Story>

## Props

<Props of={Input} />
