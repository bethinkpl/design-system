import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import TabItem, { ICONS } from 'design-system/lib/js/atoms/TabItem';

<Meta title='atoms/TabItem' component={TabItem} />

# Select

**Tab nawigacji**

<Story name='interactive'
	   args={{
		   isActive: false,
           title: 'Wpłynąłem na suchego przestówr oceanu'
	   }}
       argTypes={{
           icon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'FA_CALENDAR_ALT' },
       }}>
>
	{(args) => {
		return {
			components: { TabItem },
			props: Object.keys(args),
			template: '<tab-item :icon="ICONS[icon]" :is-active="isActive" :title="title" />',
            created() {
                this.ICONS = ICONS;
            }
        };
	}}
</Story>

## Props

<Props of={WnlSelect} />
