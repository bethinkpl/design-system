import { Meta, Story, Props } from '@storybook/addon-docs';

import { ICONS } from '../Icon';

import SurveyToggle from './SurveyToggle';
import { SURVEY_TOGGLE_COLORS, SURVEY_TOGGLE_STATES, SURVEY_TOGGLE_STATUSES } from './SurveyToggle.consts';

<Meta title='atoms/SurveyToggle' component={SurveyToggle} />

# SurveyToggle

<Story name='interactive'
	   argTypes={{
		   label: {control: { type: 'text' }, defaultValue: 'label'},
		   contentText: {control: { type: 'text' }, defaultValue: '1'},
		   color: {control: { type: 'select', options: Object.values(SURVEY_TOGGLE_COLORS) }, defaultValue: SURVEY_TOGGLE_COLORS.PRIMARY },
		   status: {control: { type: 'select', options: Object.values(SURVEY_TOGGLE_STATUSES) }, defaultValue: SURVEY_TOGGLE_STATUSES.DEFAULT },
		   state: {control: { type: 'select', options: Object.values(SURVEY_TOGGLE_STATES) }, defaultValue: SURVEY_TOGGLE_STATES.DEFAULT },
		   contentIcon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'FA_CHECK_SOLID' },
	   }}>
	{(args) => {
		return {
			components: { SurveyToggle },
			props: Object.keys(args),
			created() {
				this.ICONS = ICONS;
			},
			template: '<div style="background-color:#e5e5e5"><survey-toggle :label="label"  :color="color" :contentText="contentText" :contentIcon="ICONS[contentIcon]" :status="status" :state="state" /></div>',
		};
	}}
</Story>

## Props

<Props of={SurveyToggle} />