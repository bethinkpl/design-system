import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import Card from './Card';

<Meta title='atoms/Card' component={Card} />

# Card

<Story name='interactive'
	   argTypes={{
		   header: {control: { type: 'text' }, defaultValue: 'header slot'},
		   content: {control: { type: 'text' }, defaultValue: 'content slot that supports <b>HTML markup</b>'},
		   footer: {control: { type: 'text' }, defaultValue: 'footer slot'},
	   }}>
	{(args) => {
		return {
			components: { Card },
			props: Object.keys(args),
			template: '<card><div slot="header" v-html="header" /><div slot="content" v-html="content" /><div slot="footer" v-html="footer" /></card>',
		};
	}}
</Story>

## Props

<Props of={Card} />