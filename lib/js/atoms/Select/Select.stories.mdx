import { Meta, Story, Props } from '@storybook/addon-docs/blocks';

import { ICONS } from 'design-system/lib/js/atoms/Icon/Icon.consts';
import App from 'design-system/lib/js/atoms/App';

import Select from './Select.vue';

<Meta title='atoms/Select' component={Select} />

# Select
<Story name='interactive'
	   args={{
		   label: 'Label',
		   errorMessage: undefined,
		   error:false,
		   helpMessage: 'Help message',
		   disabled: false,
		   readonly: false,
		   id: undefined,
		   labelAddition: '(Label Addition)',
	   }}
	   >
	{(args) => {
		return {
			components: { DsSelect: Select, DsApp: App },
			data() {
				return {
					selected: undefined
				};
			},
			props: Object.keys(args),
			template: `<ds-app>
				<ds-select
					:label="label"
					:error="error"
					:disabled="disabled"
					:readonly="readonly"
					:id="id"
					v-model="selected"
					:helpMessage="helpMessage"
					:errorMessage="errorMessage"
					:labelAddition="labelAddition"
					:items="[{ value: 'ga', text: 'Georgia'}, { value: 'ne', text: 'Nebraska'}, { value: 'ca', text: 'California'}]"
				/>
			</ds-app>`,
			created() {
				this.ICONS = ICONS;
			}
		};
	}}
</Story>

<Story name='with v-model'
	   args={{
		   label: 'Label',
		   errorMessage: undefined,
		   error:false,
		   helpMessage: 'Help message',
		   disabled: false,
		   readonly: false,
		   id: undefined,
		   labelAddition: '(Label Addition)',
	   }}
>
	{(args) => {
		return {
			components: { DsSelect: Select, DsApp: App },
			data() {
				return {
					selected: 'ga'
				};
			},
			props: Object.keys(args),
			template: `<ds-app>
				<ds-select
					:label="label"
					:error="error"
					:disabled="disabled"
					:readonly="readonly"
					:id="id"
					v-model="selected"
					:helpMessage="helpMessage"
					:errorMessage="errorMessage"
					:labelAddition="labelAddition"
					:items="[{ value: 'ga', text: 'Georgia'}, { value: 'ne', text: 'Nebraska'}, { value: 'ca', text: 'California'}]"
				/>
				<div>Wybrana wartość: <strong>{{selected}}</strong></div>
			</ds-app>`,
			created() {
				this.ICONS = ICONS;
			}
		};
	}}
</Story>


## Props
<Props of={Select} />