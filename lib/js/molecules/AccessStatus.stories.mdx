import { Meta, Story } from '@storybook/addon-docs/blocks';

import WnlAccessStatus from 'lib/js/molecules/AccessStatus';
import { COURSE_ACCESS_STATUS } from 'lib/js/consts/user'

<Meta title='molecules/AccessStatus' component={ WnlAccessStatus } />

# AccessStatus

<Story name='interactive'
	args={{
		status: COURSE_ACCESS_STATUS.ACTIVE,
	}}
	argTypes={{
		status: {control: { type: 'select', options: Object.values(COURSE_ACCESS_STATUS) }, defaultValue: COURSE_ACCESS_STATUS.ACTIVE},
	}}
	>
	{(args) => {
		return {
			components: {
				WnlAccessStatus,
			},
			props: Object.keys(args),
			template: `<wnl-access-status :status="status" />`,
		};
	}}
</Story>
