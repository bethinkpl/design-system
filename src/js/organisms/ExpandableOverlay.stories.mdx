import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlExpandableOverlay from 'js/components/global/styleguide/organisms/ExpandableOverlay';

<Meta title='organisms/ExpandableOverlay' component={WnlExpandableOverlay} />

# ExpandableOverlay

<Story name='interactive'
    args={{
        shortMessage: 'Twoje zamówienie zostało anulowane ze względu na brak płatności',
    }}
    argTypes={{
		shortMessage: {control: { type: 'string' }},
	}}
    >
    {(args) => {
        return {
        	components: { WnlExpandableOverlay },
            props: Object.keys(args),
            template: `<wnl-expandable-overlay :short-message="shortMessage">
				<p>Twoje zamówienie zostało anulowane ze względu na brak płatności</p>
            </wnl-expandable-overlay>`,
        };
    }}
</Story>
