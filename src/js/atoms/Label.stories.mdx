import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlLabel from 'js/components/global/styleguide/atoms/Label';

<Meta title='atoms/Label' component={WnlLabel} />

# Label

<Story name='interactive'
    args={{
        slotText: 'Hello world',
        isOptional: false,
    }}
    >
    {(args) => {
        return {
            components: { WnlLabel },
            props: Object.keys(args),
            template: '<wnl-label :is-optional="isOptional" >{{slotText}}</wnl-label>',
        };
    }}
</Story>

## Props

<Props of={WnlLabel} />
