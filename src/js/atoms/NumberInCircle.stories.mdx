import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlNumberInCircle, { COLORS } from 'js/components/global/styleguide/atoms/NumberInCircle';

<Meta title='atoms/NumberInCircle' component={WnlNumberInCircle} />

# Label

<Story name='interactive'
    args={{
        slotText: '1',
        index: 1,
        color: COLORS.DEFAULT,
        medium: false,
    }}
    argTypes={{
        color: {control: { type: 'select', options: Object.values(COLORS) }, defaultValue: COLORS.DEFAULT},
        slotText: {control: { type: 'text' } },
    }}
>
    {(args) => {
        return {
            components: { WnlNumberInCircle },
            props: Object.keys(args),
            template: '<wnl-number-in-circle :index="index" :color="color" :medium="medium">{{slotText}}</wnl-number-in-circle>',
        };
    }}
</Story>

## Props

<Props of={WnlNumberInCircle} />
