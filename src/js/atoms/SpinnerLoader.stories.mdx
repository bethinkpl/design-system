import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import SpinnerLoader from 'js/components/global/styleguide/atoms/SpinnerLoader';

<Meta title='atoms/SpinnerLoader' component={SpinnerLoader} />

# Title

<Story name='interactive'
    args={{
        spinnerMessage: 'Some custom message',
    }}
    argTypes={{
        spinnerMessage: {control: { type: 'text' } },
    }}
    >
    {(args) => {
        return {
            components: { SpinnerLoader },
            props: Object.keys(args),
            template: '<spinner-loader>{{spinnerMessage}}</spinner-loader>',
        };
    }}
</Story>
