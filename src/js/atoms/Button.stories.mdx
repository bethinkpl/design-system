import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlButton, { SIZES, TYPES, COLORS, RADIUSES } from 'js/components/global/styleguide/atoms/Button';

<Meta title='atoms/Button' component={WnlButton} />

# Button

**Wspaniały** przycisk.

<Story name='interactive'
    args={{
    disabled: false,
    loading: false,
    size: SIZES.MEDIUM,
    type: TYPES.FILLED,
    color: COLORS.PRIMARY,
    radius: RADIUSES.CAPSULE,
    iconLeft: 'fa-chevron-left',
    iconRight: '',
    }}
    argTypes={{
        slotText: {control: { type: 'text' }, defaultValue: 'hello'},
        size: {control: { type: 'select', options: Object.values(SIZES) }, defaultValue: SIZES.MEDIUM},
        type: {control: { type: 'select', options: Object.values(TYPES) }, defaultValue: TYPES.FILLED},
        color: {control: { type: 'select', options: Object.values(COLORS) }, defaultValue: COLORS.PRIMARY},
        radius: {control: { type: 'select', options: Object.values(RADIUSES) }, defaultValue: RADIUSES.CAPSULE},
    }}>
    {(args) => {
        return {
            components: { WnlButton },
            props: Object.keys(args),
            template: '<wnl-button :size="size" :type="type" :disabled="disabled" :loading="loading" :radius="radius" :color="color" :icon-left="iconLeft" :icon-right="iconRight">{{slotText}}</wnl-button>',
        };
    }}
</Story>

## Props

<Props of={WnlButton} />
