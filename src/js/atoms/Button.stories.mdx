import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlButton, { SIZES, TYPES } from 'js/components/global/styleguide/atoms/Button';

<Meta title='atoms/Button' component={WnlButton} />

# Button

**Wspaniały** przycisk.

<Story name='interactive'
    args={{
    danger: false,
    disabled: false,
    loading: false,
    roundedCorners: true,
    size: SIZES.MEDIUM,
    type: TYPES.FILLED,
    text: false,
    }}
    argTypes={{
        slotText: {control: { type: 'text' }, defaultValue: 'hello'},
        size: {control: { type: 'select', options: Object.values(SIZES) }, defaultValue: SIZES.MEDIUM},
        type: {control: { type: 'select', options: Object.values(TYPES) }, defaultValue: TYPES.FILLED},
    }}>
    {(args) => {
        return {
            components: { WnlButton },
            props: Object.keys(args),
            template: '<wnl-button :size="size" :type="type" :danger="danger" :disabled="disabled" :loading="loading" :roundedCorners="roundedCorners">{{slotText}}</wnl-button>',
        };
    }}
</Story>

## Props

<Props of={WnlButton} />
