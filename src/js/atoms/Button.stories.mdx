import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlButton from 'js/components/global/styleguide/atoms/Button';

<Meta title='atoms/Button' component={WnlButton} />

# Button

**Wspaniały** przycisk.

<Story name='interactive'
    args={{
    danger: false,
    disabled: false,
    loading: false,
    medium: false,
    roundedCorners: true,
    secondary: false,
    small: false,
    text: false,
    }}
    argTypes={{
        slotText: {control: { type: 'text' }, defaultValue: 'hello'},
    }}>
    {(args) => {
        return {
            components: { WnlButton },
            props: Object.keys(args),
            template: '<wnl-button :danger="danger" :disabled="disabled" :loading="loading" :medium="medium" :roundedCorners="roundedCorners" :secondary="secondary" :text="text">{{slotText}}</wnl-button>',
        };
    }}
</Story>

## Props

<Props of={WnlButton} />
