import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlButton from 'js/components/global/styleguide/atoms/Button';

<Meta title='atoms/Button' component={WnlButton} />

# Button

**Wspaniały** przycisk.
d /
<Story name='interactive'
    args={{
    danger: false,
    disabled: false,
    loading: false,
    medium: false,
    roundedCorners: true,
    secondary: false,
    small: false,
    }}
    argTypes={{
        slot: {control: { type: 'text' }, defaultValue: 'hello'},
    }}>
    {(args) => {
        return {
            components: { WnlButton },
            data() {
                return args;
            },
            template: '<wnl-button :danger="danger" :disabled="disabled" :loading="loading" :medium="medium" :roundedCorners="roundedCorners" :secondary="secondary" :small="small">{{slot}}</wnl-button>',
        };
    }}
</Story>

## Props

<Props of={WnlButton} />
