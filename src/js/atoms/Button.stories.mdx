import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlButton, { SIZES, TYPES, COLORS } from 'js/components/global/styleguide/atoms/Button';

<Meta title='atoms/Button' component={WnlButton} />

# Button

**Wspaniały** przycisk.

<Story name='interactive'
    args={{
    disabled: false,
    loading: false,
    roundedCorners: true,
    size: SIZES.MEDIUM,
    type: TYPES.FILLED,
    color: COLORS.PRIMARY,
    }}
    argTypes={{
        slotText: {control: { type: 'text' }, defaultValue: 'hello'},
        size: {control: { type: 'select', options: Object.values(SIZES) }, defaultValue: SIZES.MEDIUM},
        type: {control: { type: 'select', options: Object.values(TYPES) }, defaultValue: TYPES.FILLED},
        color: {control: { type: 'select', options: Object.values(COLORS) }, defaultValue: COLORS.PRIMARY},
    }}>
    {(args) => {
        return {
            components: { WnlButton },
            props: Object.keys(args),
            template: '<wnl-button :size="size" :type="type" :disabled="disabled" :loading="loading" :roundedCorners="roundedCorners">{{slotText}}</wnl-button>',
        };
    }}
</Story>

## Props

<Props of={WnlButton} />
