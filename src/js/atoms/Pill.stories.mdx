import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlPill from 'src/js/atoms/Pill';

<Meta title='atoms/Pill' component={WnlPill} />

# Pill

<Story name='interactive'
    args={{
        slotText: 'Hello world',
        number: 0,
        backgroundColor: '#FDB72D',
    }}
    argTypes={{
        backgroundColor: {control: { type: 'color' }},
        slotText: {control: { type: 'text' } },
    }}
    >
    {(args) => {
        return {
            components: { WnlPill },
            props: Object.keys(args),
            template: '<wnl-pill :background-color="backgroundColor" :number="number" >{{slotText}}</wnl-pill>',
        };
    }}
</Story>

## Props

<Props of={WnlPill} />
