import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlIconButton, { ICON_BUTTON_SIZES, ICON_BUTTON_COLOR_SCHEMES, ICON_BUTTON_TYPES, BUTTON_ELEVATIONS, BUTTON_RADIUSES, BUTTON_COLORS, ICONS } from 'js/components/global/styleguide/atoms/IconButton';

<Meta title='atoms/IconButton' component={WnlIconButton} />

# IconButton

<Story name='interactive'
       args={{
           size: ICON_BUTTON_SIZES.MEDIUM,
           type: ICON_BUTTON_TYPES.FILLED,
           radius: BUTTON_RADIUSES.CAPSULE,
           label: 'Zamknij',
           colorScheme: ICON_BUTTON_COLOR_SCHEMES.ALL_IN_COLOR,
           touchable: true,
           elevation: BUTTON_ELEVATIONS.NONE,
           color: BUTTON_COLORS.PRIMARY,
       }}
       argTypes={{
           size: {control: { type: 'select', options: Object.values(ICON_BUTTON_SIZES) }, defaultValue: ICON_BUTTON_SIZES.MEDIUM},
           type: {control: { type: 'select', options: Object.values(ICON_BUTTON_TYPES) }, defaultValue: ICON_BUTTON_TYPES.FILLED},
           radius: {control: { type: 'select', options: Object.values(BUTTON_RADIUSES) }, defaultValue: BUTTON_RADIUSES.CAPSULE},
           icon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'FA_TIMES' },
           color: {control: { type: 'select', options: Object.values(BUTTON_COLORS) }, defaultValue: BUTTON_COLORS.PRIMARY },
           colorScheme: {control: { type: 'select', options: Object.values(ICON_BUTTON_COLOR_SCHEMES) }, defaultValue: ICON_BUTTON_COLOR_SCHEMES.ALL_IN_COLOR },
           elevation: {control: { type: 'select', options: Object.values(BUTTON_ELEVATIONS) }, defaultValue: BUTTON_ELEVATIONS.NONE },
       }}>
    {(args) => {
        return {
            components: { WnlIconButton },
            props: Object.keys(args),
            template: `<wnl-icon-button
              :icon="ICONS[icon]"
              :size="size"
              :type="type"
              :radius="radius"
              :color-scheme="colorScheme"
              :touchable="touchable"
              :elevation="elevation"
              :color="color"
            ><span v-if="label !== ''">{{label}}</span></wnl-icon-button>`,
            created() {
                this.ICONS = ICONS;
            }
        };
    }}
</Story>

## Props

<Props of={WnlIconButton} />
