import { Meta, Story, Props } from '@storybook/addon-docs/blocks';

<Meta title='atoms/RadioDeprecated' />

# Radio

<Story name='interactive'
    args={{
        disabled: false,
        checked: false,
        small: false
    }}
    argTypes={{
        disabled: {control: { type: 'boolean' }},
        checked: {control: { type: 'boolean' }},
        small: {control: { type: 'boolean' }},
    }}
    >
    {(args) => {
        return {
            props: Object.keys(args),
            template: '<input type="radio" class="a-radioDeprecated" :class="{ \'-small\': small }" :disabled="disabled" :checked="checked" />',
        };
    }}
</Story>
