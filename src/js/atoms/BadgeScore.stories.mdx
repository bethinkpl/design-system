import { Meta, Story, Props } from '@storybook/addon-docs/blocks';

import BadgeScore, { BADGE_SCORE_COLORS} from './BadgeScore.vue';

<Meta title='atoms/BadgeScore' component={BadgeScore} />

# BadgeScore

<Story name='interactive'
	   args={{
		   text: '42%',
		   color: BADGE_SCORE_COLORS.SUCCESS,
	   }}
	   argTypes={{
		   text: { control: { type: 'text' } },
		   color: {
			   control: { type: 'select', options: Object.values(BADGE_SCORE_COLORS) }
		   }
	   }}>
	{(args) => {
		return {
			components: { BadgeScore },
			props: Object.keys(args),
			template: '<badge-score :color="color" :text="text" />',
		};
	}}
</Story>

## Props

<Props of={BadgeScore} />
