import { Meta, Story, Props, IconGallery, IconItem } from '@storybook/addon-docs/blocks';
import WnlIcon, {ICON_SIZES, ICONS} from 'src/js/atoms/Icon';

<Meta title='atoms/Icon' component={WnlIcon} />

# Icon

<Story name='interactive'
       args={{
           size: ICON_SIZES.MEDIUM,
           icon: 'FILE_VERIFIED',
           spinning: false,
           touchable: false,
           rotation: null
       }}
       argTypes={{
           size: {control: { type: 'select', options: Object.values(ICON_SIZES) }, defaultValue: ICON_SIZES.MEDIUM},
           icon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'FILE_VERIFIED'},
           rotation: {control: { type: 'select', options: [null, 90, 180, 270] }, defaultValue: null},
       }}>
    {(args) => {
        return {
            components: { WnlIcon },
            props: Object.keys(args),
            created() {
                this.ICONS = ICONS
            },
            template: '<div style="display: flex"><wnl-icon :icon="ICONS[icon]" :size="size" :touchable="touchable" :spinning="spinning" :rotation="rotation" /></div>',
        };
    }}
</Story>

<Story name='all icons'>
    {() => {
        return {
            components: { WnlIcon },
            created() {
                this.ICONS = ICONS
            },
            template: '<div style="display: flex; flex-wrap: wrap; font-size: 10px">' +
                '<div v-for="(icon, iconName) in ICONS" :key="iconName" style="display: flex; flex-direction: column; align-items: center; padding: 10px; width: 150px; word-break: break-word; text-align: center">'+
                  '<wnl-icon :icon="icon" />' +
                  '<div>{{iconName}}</div>' +
                '</div>' +
            '</div>',
        };
    }}
</Story>

## Props

<Props of={WnlIcon} />
