import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlIcon, {ICON_SIZES} from 'js/components/global/styleguide/atoms/Icon';

<Meta title='atoms/Icon' component={WnlIcon} />

# Icon

<Story name='interactive'
    args={{
    size: ICON_SIZES.MEDIUM,
    faIconClass: 'trash-alt',
    iconSet: 'fas',
    touchable: false,
    }}
    argTypes={{
        size: {control: { type: 'select', options: Object.values(ICON_SIZES) }, defaultValue: ICON_SIZES.MEDIUM},
    }}>
    {(args) => {
        return {
            components: { WnlIcon },
            props: Object.keys(args),
            template: '<div style="display: flex"><wnl-icon :fa-icon-class="faIconClass" :icon-set="iconSet" :size="size" :touchable="touchable" /></div>',
        };
    }}
</Story>

## Props

<Props of={WnlIcon} />
