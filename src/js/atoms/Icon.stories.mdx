import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlIcon, {ICON_SIZES, ICONS} from 'js/components/global/styleguide/atoms/Icon';

<Meta title='atoms/Icon' component={WnlIcon} />

# Icon

<Story name='interactive'
       args={{
           size: ICON_SIZES.MEDIUM,
           icon: 'FILE_VERIFIED',
           touchable: false,
       }}
       argTypes={{
           size: {control: { type: 'select', options: Object.values(ICON_SIZES) }, defaultValue: ICON_SIZES.MEDIUM},
           icon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'FILE_VERIFIED'},
       }}>
    {(args) => {
        return {
            components: { WnlIcon },
            props: Object.keys(args),
            created() {
 this.ICONS = ICONS;
                this.ICONS = ICONS
            },
            template: '<div style="display: flex"><wnl-icon :icon="ICONS[icon]" :size="size" :touchable="touchable" /></div>',
        };
    }}
</Story>

<Story name='all icons'>
    {() => {
        return {
            components: { WnlIcon },
            created() {
 this.ICONS = ICONS;
                this.ICONS = ICONS
            },
            template: '<div style="display: flex; flex-wrap: wrap">' +
                '<div v-for="(icon, iconName) in ICONS" :key="iconName" style="display: flex; flex-direction: column; align-items: center; padding: 10px">'+
                  '<wnl-icon :icon="icon" />' +
                  '<div>{{iconName}}</div>' +
                '</div>' +
            '</div>',
        };
    }}
</Story>

## Props

<Props of={WnlIcon} />
