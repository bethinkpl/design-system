import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlIcon, {SIZES} from 'js/components/global/styleguide/atoms/Icon';

<Meta title='atoms/Icon' component={WnlIcon} />

# Icon

<Story name='interactive'
    args={{
    size: 'small',
    faIconClass: 'fa-trash',
    touchable: false,
    }}
    argTypes={{
        size: {control: { type: 'select', options: Object.values(SIZES) }, defaultValue: SIZES.MEDIUM},
    }}>
    {(args) => {
        return {
            components: { WnlIcon },
            props: Object.keys(args),
            template: '<div style="display: flex"><wnl-icon :fa-icon-class="faIconClass" :size="size" :touchable="touchable" /></div>',
        };
    }}
</Story>

## Props

<Props of={WnlIcon} />
