import { Meta, Story } from '@storybook/addon-docs/blocks';

<Meta title='molecules/FormGroup' />

# FormGroup

<Story name='interactive'
    args={{
        label: 'Twój e-mail',
        isError: false,
        error: 'To pole jest polem wymaganym.',
    }}
    argTypes={{
        label: {control: { type: 'text' }},
        isError: {control: { type: 'boolean' }},
        error: {control: { type: 'text' }},
    }}
    >
    {(args) => {
        return {
            props: Object.keys(args),
            template: `<div class="m-formGroup">
               <label for="email" class="a-label -required">{{label}}</label>
               <input class="a-input" :class="{ '-error': isError }" required="required" name="email" type="email" value="" id="email">
               <div v-if="isError" class="a-error">{{error}}</div>
            </div>`,
        };
    }}
</Story>
