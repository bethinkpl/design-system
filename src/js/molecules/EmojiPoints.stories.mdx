import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlEmojiPoints from 'js/components/global/styleguide/molecules/EmojiPoints';

<Meta title='molecules/EmojiPoints' component={WnlEmojiPoints} />

# EmojiPoints

<Story name='interactive'
    args={{
        items: [
            {
                emoji: '⏰',
                text: 'Test 123'
            },
            {
                emoji: '💡',
                text: 'halo halo'
            },
            {
                emoji: '📅',
                text: 'to jeszcze ostatni punkt'
            }
        ],
        large: true,
        small: false,
        medium: false,
        vertical: false,
        separated: false,
    }}
    >
    {(args) => {
        return {
            components: { WnlEmojiPoints },
            props: Object.keys(args),
            template: '<wnl-emoji-points :items="items" :large="large" :small="small" :medium="medium" :vertical="vertical" :separated="separated" />',
        };
    }}
</Story>

## Props

<Props of={WnlEmojiPoints} />
