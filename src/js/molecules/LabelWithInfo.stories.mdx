import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlLabelWithInfo from 'js/components/global/styleguide/molecules/LabelWithInfo';
import WnlLabel from 'js/components/global/styleguide/atoms/Label';
import WnlButton from 'js/components/global/styleguide/atoms/Button';
import WnlIcon, { ICONS } from 'js/components/global/styleguide/atoms/Icon';

import WnlModal from 'js/components/global/Modal';
import WnlModalParagraph from 'js/components/global/modals/content/Paragraph';

<Meta title='molecules/LabelWithInfo' component={WnlLabelWithInfo} />

# WnlLabelWithInfo

<Story name='interactive'>
    {(args) => {
        return {
            components: { WnlLabelWithInfo, WnlLabel, WnlButton, WnlIcon, WnlModal, WnlModalParagraph },
            props: Object.keys(args),
            data() {
                return {
                    languageModalVisible: false,
                };
            },
            template: `<wnl-label-with-info>
                <wnl-label input-id="finished_studies">Czy studia masz już za sobą? W jakim języku?</wnl-label>
                <template slot="info">
                    <wnl-icon :icon="ICONS.QUESTION_CIRCLE" @click.native="languageModalVisible = true" />
                    <wnl-modal v-if="languageModalVisible" @close-modal="languageModalVisible = false">
                    		<h4 slot="header" class="-headlineXL -textBold">Dlaczego pytamy o język?</h4>
                    		<template>
                    			<wnl-modal-paragraph>
                    				Ukończenie studiów w języku polskim jest jednym z warunków skorzystania z Gwarancji Satysfakcji.
                    			</wnl-modal-paragraph>
                    		</template>
                    		<wnl-button slot="footer" @click.native="languageModalVisible = false">Ok, Rozumiem</wnl-button>
                    	</wnl-modal>
                </template>
            </wnl-label-with-info>`,
            created() {
                this.ICONS = ICONS;
            },
        };
    }}
</Story>

## Props

<Props of={WnlLabelWithInfo} />
