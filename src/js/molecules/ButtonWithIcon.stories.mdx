import { Meta, Story } from '@storybook/addon-docs/blocks';
import WnlIcon from 'js/components/global/styleguide/atoms/Icon';
import WnlButtonWithIcon from 'js/components/global/styleguide/molecules/ButtonWithIcon';

<Meta title='molecules/ButtonWithIcon' component={ WnlButtonWithIcon } />

# ButtonWithIcon

<Story name='interactive'
	args={{
		danger: false,
		disabled: false,
		loading: false,
		roundedCorners: true,
		secondary: true,
		text: false,
	}}
    argTypes={{
        danger: {control: { type: 'boolean' }},
        disabled: {control: { type: 'boolean' }},
        loading: {control: { type: 'boolean' }},
        roundedCorners: {control: { type: 'boolean' }},
        secondary: {control: { type: 'boolean' }},
        text: {control: { type: 'boolean' }},
    }}
>
    {(args) => {
        return {
        	components: {
				WnlButtonWithIcon,
				WnlIcon,
        	},
        	props: Object.keys(args),
            template:
`<wnl-button-with-icon :danger="danger" :disabled="disabled" :loading="loading" :roundedCorners="roundedCorners" :secondary="secondary" :small="small" :text="text">
	Tekst przycisku
	<template #icon>
		<wnl-icon
			fa-icon-class="fa-angle-down"
			size="medium"
		/>
	</template>
</wnl-button-with-icon>`,
        };
    }}
</Story>
