import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlCircle from 'js/components/global/styleguide/molecules/Circle';

<Meta title='molecules/Circle' component={WnlCircle} />

# Circle

<Story name='interactive'
    args={{
        slotText: '<span class=-mischkaGray">2</span>',
    }}
    argTypes={{
        slotText: {control: { type: 'text' }},
    }}
    >
    {(args) => {
        return {
            components: { WnlCircle },
            props: Object.keys(args),
            template: '<wnl-circle><span v-html="slotText" /></wnl-circle>',
        };
    }}
</Story>

## Props

<Props of={WnlCircle} />
