import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import WnlIconButtonWithLabel, { ICONS } from 'js/components/global/styleguide/molecules/IconButtonWithLabel';

<Meta title='molecules/IconButtonWithLabel' component={WnlIconButtonWithLabel} />

# IconButtonWithLabel

<Story name='interactive'
       args={{
           label: 'Zamknij',
       }}
       argTypes={{
           icon: {control: { type: 'select', options: Object.keys(ICONS) }, defaultValue: 'TIMES' },
       }}>
>
    {(args) => {
        return {
            components: { WnlIconButtonWithLabel },
            props: Object.keys(args),
            template: `<wnl-icon-button-with-label :label="label" :icon="ICONS[icon]" />`,
            created() {
                this.ICONS = ICONS;
            }
        };
    }}
</Story>

## Props

<Props of={WnlIconButtonWithLabel} />
